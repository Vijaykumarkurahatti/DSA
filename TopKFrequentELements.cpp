/*
- take n as numbers size
- take unorder map m
- for loop on i 0 to n
       - map of number [i] ++
- take vectot vector int buckets n +1

for loop on map
    - bucket[it -> 2] push it firsy

- take vector result
for loop on i =n to > = to 0 i--
   - if result size >= k
       breal for loop
   - if not bucket of i empty 
        - result insert (result end , bucket of i begin, bucket of i end)

  last return resukt
  


*/
