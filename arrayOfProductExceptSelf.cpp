/*
--    take n nums size
--    take result vector int  with n size and all 1
--     take prefix as 1
--     for loop i -0 to n
        - assign each result [i] to prefix
        - prefix to prefix x  each nuber[i]
        
       - tske postfix=1
       for loop on ( from n-1 to i > = 0 i--)
       - assign res[i] to reslt[i] * postfix
       - psotfix = postfix x number of i

       return the result
       
        


*/
