/*
Encode
- Take result string
- for loop on strings
      take one string
      result +=  to dtring(one str size) + # + one sstr;
   return result

Decode
- vector result(string)
while on i until string size
     - j is i
     while until s[j] not = #
       -j++
    - tak lenth  str to int convert (sub string  (i , j-i)
    - str = sub string j+1, length
    - result push str
    - icreement i to  i +1 +length

return result

*/
